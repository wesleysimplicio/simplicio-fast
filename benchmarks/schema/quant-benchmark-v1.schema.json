{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://simplicio.dev/schemas/fast/quant-benchmark-v1.schema.json",
  "title": "Simplicio Fast quant benchmark receipt",
  "type": "object",
  "required": [
    "schema",
    "issue",
    "status",
    "classification",
    "config_hash",
    "generation",
    "environment",
    "corpora",
    "measured",
    "unavailable_sizes",
    "simulated",
    "parity",
    "claims"
  ],
  "properties": {
    "schema": {
      "const": "simplicio.fast.quant-benchmark/v1"
    },
    "issue": {
      "const": 198
    },
    "status": {
      "const": "measured"
    },
    "classification": {
      "const": "MEASURED"
    },
    "config_hash": {
      "$ref": "#/$defs/sha256"
    },
    "generation": {
      "$ref": "#/$defs/sha256"
    },
    "measured": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "classification",
          "vectors",
          "dataset",
          "lanes",
          "promotion_gate"
        ],
        "properties": {
          "classification": {
            "const": "MEASURED"
          },
          "vectors": {
            "type": "integer",
            "minimum": 1
          },
          "lanes": {
            "type": "object",
            "required": [
              "Q0",
              "Q1",
              "Q2a",
              "Q2b"
            ]
          }
        }
      }
    },
    "unavailable_sizes": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "classification",
          "vectors",
          "value",
          "reason"
        ],
        "properties": {
          "classification": {
            "const": "MEASURED"
          },
          "value": {
            "type": "null"
          },
          "reason": {
            "const": "CAPACITY_LIMIT_CONFIGURED"
          }
        }
      }
    },
    "simulated": {
      "type": "object",
      "required": [
        "classification",
        "values",
        "reason"
      ],
      "properties": {
        "classification": {
          "const": "SIMULATED"
        },
        "values": {
          "type": "null"
        },
        "reason": {
          "const": "SIMULATION_NOT_RUN"
        }
      }
    }
  },
  "$defs": {
    "sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    }
  },
  "additionalProperties": true
}
